// Single source of truth for the site's own URL. The domain is not registered yet —
// change this one constant when it is, never hardcode "macote.xyz" anywhere else.
export const SITE_URL = "https://macote.xyz";
export const SITE_DOMAIN = "macote.xyz";
